import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titleText: {
        fontSize:18,
        fontWeight:'600',
        color:'black',
        textAlign:'center',
        // alignSelf:'center'
    },
    flexRow: {
        flexDirection:'row',
        alignItems:'center',
        // alignSelf:'center'
    }
});

export default styles;